#pragma once

// Entry point wired into OthersMenu. Settings are reached from inside the
// module itself (its first screen, or typing "/settings" while chatting),
// not as a separate top-level menu entry.
void bruceLM_setup();
